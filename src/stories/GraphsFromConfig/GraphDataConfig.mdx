import { Meta } from '@storybook/blocks';
import { ChartConfigDropdown } from '../Dashboards/Elements/ChartConfigDropdown';

<Meta title='Misc./Data config' />

# Graph Config

Select a chart type below to see its configuration format.

<ChartConfigDropdown type='dataSettings'/>
